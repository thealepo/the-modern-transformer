# baseline.yaml: this is the GPT2 baseline that everything compares against

# dims (GPT-2 small)
vocab_size: 50257
seq_len: 256
hidden_size: 768
n_heads: 12
n_layers: 12

# slots
norm: layernorm
positional: learned
ffn: gelu_mlp
attention: mha

n_kv_heads: null
mlp_ratio: 4